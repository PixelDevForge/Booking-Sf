<nav class="navbar navbar-expand-lg bg-primary" data-bs-theme="dark">
  <div class="container-fluid">
      <a class="navbar-brand" href="{{ path('app_home') }}">Home</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01"
          aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarColor01">
          <ul class="navbar-nav me-auto">
              <li class="nav-item">
                  <a class="nav-link" href="{{ path('app_ads_list') }}">Liste</a>
              </li>
          </ul>
          <ul class="navbar-nav ml-auto">
              {% if app.user %}
              <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown"
                    aria-haspopup="true" aria-expanded="false" id="accountDropdownLink">
                    <img src="{{ app.user.avatar }}" alt="Avatar de {{ app.user.fullname }}"
                        class="avatar avatar-small">
                    {{ app.user.fullname }}
                </a>
                <div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDropdownLink">
                    <a class="dropdown-item" href="{{ path('app_account_home') }}">Account Home</a>
                    <a class="nav-link" href="{{ path('ads_create') }}"> Create</a>
                    <a class="dropdown-item" href="{{ path('app_account_profile') }}">Profile</a>
                    <a class="dropdown-item" href="{{ path('app_account_bookings') }}">Mes réservations</a>
                    <div class="dropdown-divider"></div>
                    <a class="dropdown-item" href="{{ path('app_account_logout') }}">Logout</a>
                </div>
              </li>
              {% else %}
              <li class="nav-item">
                  <a class="nav-link" href="{{ path('app_account_register') }}">Inscription</a>
              </li>
              <li class="nav-item">
                  <a class="nav-link" href="{{ path('app_account_login') }}">Login</a>
              </li>
              {% endif %}
          </ul>
      </div>
  </div>
</nav>
